name: Run tests

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        chrome-version: [99.0.4844.82-1]
        node-version: [10.19.0]

    steps:
      - uses: actions/checkout@v2
      - name: Update submodules
        run: git submodule update --init
      - uses: actions/setup-node@v2
        with:
          node-version: ${{ matrix.node-version }}
      - name: Install Chrome
        run: |
          wget -O /tmp/chrome.deb https://dl.google.com/linux/chrome/deb/pool/main/g/google-chrome-stable/google-chrome-stable_${{ matrix.chrome-version }}_amd64.deb
          sudo apt update
          sudo apt install -y /tmp/chrome.deb
      - name: Install dependencies
        run: | 
          cd lib/jsorolla
          npm install
          cd ../..
          npm install
      - name: Run tests
        run: |
          npm install -g grunt-cli
          sudo apt install xvfb && Xvfb :99 & export DISPLAY=:99
          grunt --env=staging
